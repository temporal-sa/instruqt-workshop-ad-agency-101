version: "3"
containers:
- name: workstation
  # Rebuild + push on content changes (bump the tag, update this ref):
  #   docker build --platform linux/amd64 -f instruqt/Dockerfile -t mjones2219/temporal-python-workshop:vN .
  #   docker push mjones2219/temporal-python-workshop:vN
  image: mjones2219/temporal-python-workshop:v13
  shell: /bin/bash
  ports:
  - 8233
  - 8000
  memory: 2048
