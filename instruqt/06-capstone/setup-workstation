#!/bin/bash
set -euxo pipefail
# The capstone must run through the outage: chaos ON. Ignore failure: if
# AdNet isn't up yet it boots with chaos on anyway.
curl -sf -X POST localhost:9999/chaos/on || true
